# Resume Creator

## Start Frontend + Backend Together

From the repository root:

```bash
npm run dev
```

This starts:
- Node backend at `http://localhost:8000`
- Vite frontend at `http://localhost:5173`

Use `Ctrl+C` once to stop both processes.
